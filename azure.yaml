# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: remote-mcp-aspnetcore
metadata:
  template: remote-mcp-aspnetcore@1.0.0
services:
  api:
    project: ./src/api
    language: csharp
    host: appservice
